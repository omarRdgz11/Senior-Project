windssight.com, www.windssight.com {
    encode gzip

    # API routes -> backend container
    handle_path /api/* {
        rewrite * {path}
        reverse_proxy backend:5005
    }

    # Everything else -> built frontend
    root * /srv
    try_files {path} /index.html
    file_server
}
