
# Senior-project Directory Structure
├─ .env                             # DB creds + DATABASE_URL (db host), VITE_API_BASE
├─ .gitignore
├─ .dockerignore
├─ docker-compose.yml               # db, adminer, backend (5005), frontend (5173)
├─ README.md                        # (optional) quickstart / team notes
│
├─ backend/
│  ├─ Dockerfile                    # Flask dev server on 0.0.0.0:5005
│  ├─ requirements.txt              # Flask, SQLAlchemy, psycopg, Flask-Migrate, etc.
│  │
│  └─ app/
│     ├─ __init__.py                # create_app(); loads env; db.init_app; migrate.init_app; register_routes
│     ├─ extensions.py              # db = SQLAlchemy(); migrate = Migrate()
│     │
│     ├─ Models/                    # NOTE: capital M (case-sensitive in Linux)
│     │  ├─ __init__.py             # from .message import Message; from .raw_detection import RawDetection
│     │  ├─ message.py              # Message model (id, text, created_at)
│     │  └─ raw_detection.py        # RawDetection model (your table)
│     │
│     ├─ Routes/                    # NOTE: capital R (case-sensitive in Linux)
│     │  ├─ __init__.py             # register_routes(app) -> health_bp, hello_bp, raw_detections_bp
│     │  ├─ health.py               # GET /api/ping, GET /api/db-health
│     │  ├─ hello.py                # GET/POST /api/hello
│     │  └─ raw_detections.py       # GET /api/raw-detections, GET /api/raw-detections/<id>
│     │
│  Migrations/                # Alembic (Flask-Migrate) state lives here
│        ├─ README                  # auto-generated by alembic init
│        ├─ env.py                  # Alembic env; ties into Flask app/db
│        ├─ script.py.mako          # migration script template
│        └─ versions/               # migration scripts
│           ├─ 2e6e7f32d36e_baseline_after_reset.py
│           └─ e1f0cbe01139_true_raw_detection_schema.py
│
└─ frontend/
   ├─ README.md
   ├─ package.json
   ├─ vite.config.ts                # Vite + @tailwindcss/vite + React; /api proxy
   ├─ index.html                    # sets default daisyUI theme via data-theme
   ├─ tailwind.config.js            # Tailwind (ESM) + optional daisyUI plugin
   └─ src/
      ├─ index.css                  # Tailwind v4 + @plugin "daisyui"; minimal globals
      ├─ main.tsx                   # React bootstrap; RouterProvider
      ├─ vite-env.d.ts
      │
      ├─ api/
      │  └─ rawDetections.ts        # fetch helpers for /api/raw-detections
      │
      ├─ routes/
      │  └─ app-routes.tsx          # createBrowserRouter + nested routes
      │
      ├─ layouts/
      │  └─ AppShell.tsx            # navbar + <Outlet/> + footer; daisyUI tokens
      │
      ├─ components/
      │  └─ Page.tsx                # simple page wrapper (optional)
      │
      └─ pages/
         ├─ Home/
         │  └─ HomePage.tsx
         ├─ About/
         │  ├─ AboutPage.tsx
         │  └─ People/
         │     ├─ Omar.tsx
         │     └─ Teammate.tsx
         └─ Test/
            └─ TestRawDetections.tsx  # “test page” to verify API/data flow

